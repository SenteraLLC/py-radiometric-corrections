name: correct6x-venv
channels:
  - conda-forge
dependencies:
  - python=3.8
  # dev dependencies
  - pytest
  - pre_commit
  - sphinx
  - sphinx_rtd_theme
  - m2r
  - pip
  - pipenv
  # main dependencies
  - Pillow
  - numpy
  - tifffile
  - pandas
  - tqdm
  # subpackage dependencies
  # Sentera dependencies
  - pip:
    - git+https://github.com/SenteraLLC/py-image-metadata-parser.git@v1.9.3
    - git+https://github.com/SenteraLLC/py-image-registration.git@v1.1.1
